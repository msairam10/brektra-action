{
  "name": "brektra-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action: run a Brektra security scan against a PR preview and fail the build on confirmed exploits.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify --license LICENSES.txt",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.5.0"
  }
}
